<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leshtelsentunisie">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Signup">
            </activity>
        <activity android:name=".SignIn" ><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivityDarezzahra"
            android:label="@string/title_activity_maps_darezzahra" />
        <activity android:name=".Darezzahra_MainActivity" />
        <activity
            android:name=".MapsActivityDarsaliam"
            android:label="@string/title_activity_maps_darsaliam" />
        <activity android:name=".Darsalima_MainActivity" />
        <activity android:name=".List_Hotel_Benarous_Main_Activity" />
        <activity
            android:name=".MapsActivityAlrawabi"
            android:label="@string/title_activity_maps_alrawabi" />
        <activity android:name=".Alrawabi_MainActivity" />
        <activity
            android:name=".MapsActivityRamsam"
            android:label="@string/title_activity_maps_ramsam" />
        <activity android:name=".Ramsam_MainActivity" />
        <activity android:name=".List_Hotel_Beja_Main_Activity" />
        <activity android:name=".Samron_MainActivity" />
        <activity
            android:name=".MapsActivityPenthouse"
            android:label="@string/title_activity_maps_penthouse" />
        <activity android:name=".Penthouse_MainActivity" />
        <activity
            android:name=".MapsActivitySamron"
            android:label="@string/title_activity_maps_samron" />
        <activity android:name=".List_Hotel_Ariana_Main_Activity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>